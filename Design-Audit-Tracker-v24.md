# Критичний дизайн-аудит: «12 тижнів — трекер цілей»

**URL:** https://kazmirchuk96.github.io/tracker/ · **Білд:** v24 · **Дата:** 29.07.2026
**Метод:** живий рендер у Chrome (desktop 1440px і 500px), заміри через `getComputedStyle`,
інвентаризація значень по всьому DOM (471 елемент).

**Позиція рецензента:** оцінюю за планкою Linear / Stripe / Vercel / Arc 2025–2026,
а не «для себе достатньо», як і просив.

---

## 1. Загальна оцінка

# 5.4 / 10

Це **добре зроблений інструмент із посереднім дизайном**. Інженерна частина —
токени, ARIA, керування фокусом, покриття тестами — на рівні, якого більшість
пет-проєктів не досягає ніколи. Але візуальна мова — типовий «темний дашборд»,
який можна зібрати з будь-якого UI-кіту за вечір. Нічого в ньому не змушує
сказати «хто це робив?».

Три речі тримають оцінку нижче шістки, і всі три — системні, не косметичні:

1. **Немає моделі глибини.** Заміряв: `box-shadow` мають **2 елементи з 471**.
   Єдина справжня тінь — `0 8px 24px rgba(0,0,0,0.12)` на панелі меню, і вона
   технічно невидима: 12% чорного поверх фону `#0F0E0A` не дає жодного
   візуального ефекту. Це значення з світлої теми, механічно перенесене в темну.
2. **Немає системи відступів.** Порахував по всьому DOM: **18 різних значень**,
   серед них `5px`, `7px`, `11px`, `17.6px`, `19.2px`. Жодної сітки — ні 4px, ні 8px.
   Іронія в тому, що радіуси, шрифти, рамки й кольори токенізовані бездоганно,
   а відступи — найпомітніша система в преміальному UI — лишились хаосом.
3. **Немає типографічного ритму.** `line-height: normal` **на всіх** перевірених
   елементах: h1, головне число, підписи метрик, заголовки секцій. Тобто
   міжрядковий інтервал ніхто ніколи не задавав — його визначає браузер.

Плюс одна знахідка, яку мушу назвати прямо: **кнопка «підставити» має висоту 31px**
при заявленому мінімумі 44px. Тобто попередня робота над тап-цілями цю кнопку
пропустила, а вона — одна з найчастіше вживаних у щоденному сценарії.

---

## 2. Детальна оцінка за критеріями

### 2.1. Візуальна ієрархія — 4 / 10

**Добре:** головне число 42px у Fraunces проти 16px Inter довкола — контраст
розмірів працює. Кольорове кодування «зелений / жовтий» зчитується миттєво.

**Погано:** ієрархія **інвертована на головному екрані**. Одинадцять кружечків
майбутніх тижнів залиті насиченим `#F0B94D` на повну — і разом вони займають
більше візуальної ваги, ніж картка поточного тижня, заради якої відкривають
застосунок. Порожній стан кричить голосніше за єдиний змістовний.

Друга проблема: `.hero-num` і `h1` мають **однаковий розмір 22px** на вузькому
екрані (перевірив заміром). Тобто на телефоні головна метрика тижня і назва
застосунку — однакової ваги. Назва застосунку не повинна конкурувати ні з чим.

**Як виправити:** майбутні тижні — прозорий кружечок з обведенням, без заливки;
залишити заливку тільки для тижнів із даними. `h1` опустити до 17–18px,
`.hero-num` на мобільному тримати мінімум 32px.

---

### 2.2. Композиція — 5 / 10

**Добре:** одноколонковий потік з обмеженням 720px — правильне рішення,
не розтягується на весь монітор.

**Погано:** на десктопі 1440px колонка 720px лишає **по 360px порожнечі з
кожного боку**, і ця порожнеча нічим не працює. Це не «повітря», це невикористаний
екран. Linear і Vercel у такій ситуації або дають контенту дихати ширше, або
підвішують допоміжну навігацію збоку.

Всередині картки метрики — гірше. Поле вводу займає близько **500px ширини для
трьох-чотирьох символів**. Візуально це велика порожня плита, у лівому краю якої
загубилось «84,6». Композиційно це найслабше місце всього інтерфейсу.

**Як виправити:** обмежити поле вводу 140–160px, решту простору віддати
кнопці «підставити» з повним підписом або перенести туди контекст плану.

---

### 2.3. Відступи — 3 / 10

Найнижча оцінка розділу, і вона заслужена.

**Заміри по всьому DOM:**
```
8px×37  2px×30  5px×29  12px×15  10px×15  6px×13  14px×8
4px×6   17.6px×4  24px×3  7px×3  11px×3  3px×3  16px×3  19.2px×2  20px  18px
```

Вісімнадцять значень. `5px` зустрічається 29 разів — це не випадковість, це
звичка. `17.6px` і `19.2px` — наслідок `rem`-значень (`1.1rem`, `1.2rem`),
які дають нецілі пікселі. `.card` має `padding: 14.4px 19.2px`, `.hero-card` —
`17.6px 19.2px`. Дві сусідні картки з різними вертикальними відступами
без жодної причини.

**Як виправити:** ввести `--sp-1…--sp-8` на 4px-сітці (4/8/12/16/24/32/48/64)
і прогнати весь файл, як робили з радіусами. Це та сама операція, просто
її не довели до кінця.

---

### 2.4. Типографіка — 4 / 10

**Добре:** пара Fraunces + Inter — свідомий вибір, не дефолт. Шкала розмірів
токенізована (6 кроків). `font-variant-numeric: tabular-nums` на числах — деталь,
яку більшість забуває.

**Погано, і це серйозно:**

- **`line-height: normal` на всьому.** Перевірив h1, `.hero-num`, `.metric-label`,
  `.section-label` — жоден не має заданого міжрядкового. Для «преміального»
  рівня це базовий провал: саме line-height створює вертикальний ритм.
- **Fraunces підключений як варіативний шрифт з віссю оптичного розміру
  (`opsz@9..144`), але `font-optical-sizing` і `font-variation-settings`
  не використовуються жодного разу** (перевірив: 0 збігів). Тобто завантажується
  вага, за яку не платять користю. Fraunces має ще осі SOFT і WONK — теж не задіяні.
- `letter-spacing: -0.22px` на 22px тексті — це `-0.01em`, застосований однаково
  до заголовка й до числа. Великі числа зазвичай потребують сильнішого
  негативного трекінгу, дрібний текст — навпаки, позитивного.

**Як виправити:** line-height 1.1 для дисплейних чисел, 1.25 для заголовків,
1.5 для тексту. Увімкнути `font-optical-sizing: auto`. Розвести трекінг:
`-0.03em` на 42px, `0` на 16px, `+0.01em` на 11.5px.

---

### 2.5. Кольорова палітра — 6 / 10

**Добре:** тепла темна база (`#0F0E0A` з жовтуватим підтоном, не мертвий
сірий) — приємно й нетипово. Семантичні токени після минулої ітерації
на місці.

**Погано:** **семантична колізія жовтого.** `#F0B94D` означає одночасно
«тиждень у процесі», «темп відстає» і «попередження про дивну суму».
Три різні за смислом стани одним кольором — користувач не може за кольором
зрозуміти, добре це чи погано.

Далі: палітра має **три акцентні кольори для метрик** (фіолетовий `#9089E0`
вага, синій `#5BA3E8` борг, зелений `#2FBE93` капітал), і зелений метрики
збігається з зеленим «успіху». Тобто капітал завжди виглядає «успішним»
незалежно від стану.

**Як виправити:** розвести жовтий на два відтінки — нейтральний «у процесі»
(ближче до сірого) і тривожний «відстаєш» (насиченіший, ближче до бурштину).
Колір капіталу зсунути в бірюзу, щоб не збігався з семантикою успіху.

---

### 2.6. Контрастність — 8 / 10

Найсильніший критерій, бо минула ітерація саме цим і займалась. Перерахував:
`--warn-text` 9.66:1, `--border-strong` 3.96:1, основний текст 17.06:1.

**Погано:** `--text-muted` (`#8C8977`) на `--surface-2` дає **4.66:1** — проходить
норму 4.5:1 з запасом у 3%. Це не помилка, але це край, і саме цим кольором
набрані найдрібніші підписи (11.5px). Дрібний текст на межі контрасту —
поєднання двох ризиків одразу.

---

### 2.7. Білий простір — 5 / 10

**Добре:** між секціями дихається нормально.

**Погано:** простір розподілений нерівномірно. Всередині поля вводу —
надлишок (500px під 4 символи), між рядками метрик — 8px, що замало для
візуального відокремлення трьох різних сутностей. Простір є, але він
не працює на структуру: не читається, що вага/борг/капітал — три
рівноправні незалежні блоки.

---

### 2.8. Баланс елементів — 4 / 10

Три великі помилки балансу, усі видно на першому екрані:

1. Стрічка з 12 яскравих кружечків важить більше за картку тижня.
2. Порожні поля вводу — три великі темні прямокутники поспіль.
3. Підпис «Weekly Execution Score — 5 дій тижня: зважування, облік їжі,
   калорії, конверт на борг, переказ на капітал» займає **три рядки під
   головним числом**. Пояснення методики важить більше за саму метрику.
   Це текст для першого запуску, а не для щоденного показу.

---

### 2.9. Консистентність — 7 / 10

**Добре:** після токенізації радіуси, шрифти, рамки й кольори справді
консистентні. Іконки зведені в одне джерело.

**Погано:** консистентність **розмірів контролів порушена**: `.fill-btn` — 31px
проти 44px у всіх інших. І та сама кнопка має власний фон `--surface-0`,
тоді як сусідня `.icon-btn` прозора. Дві кнопки в одному рядку за різними
правилами.

---

### 2.10. Сучасність дизайну — 4 / 10

Виглядає як дашборд 2021 року, зроблений акуратно. Немає жодного прийому
з арсеналу 2025–2026: ні шарів глибини, ні м'яких градієнтних підсвіток,
ні текстур, ні продуманої роботи зі світлом. Плаский — але не в сенсі
«мінімалістичний», а в сенсі «недороблений». Різниця в тому, що мінімалізм
Linear тримається на бездоганному ритмі й глибині; тут ні того, ні іншого.

---

### 2.11. UX — 8 / 10

Найсильніша частина продукту. Клікабельний таймлайн, навігація по тижнях,
підстановка плану, прев'ю перед скиданням, миттєвий рендер із кешу —
усе це продумано й працює. Зауваження: після перших запусків підпис
методики варто ховати, а «Налаштувати цілі» й «Налаштувати синхронізацію»
логічно об'єднати.

---

### 2.12. Читабельність — 6 / 10

Розміри вирівняні, контраст виправлений. Але `line-height: normal`
на багаторядкових підписах (той самий Weekly Execution Score на три рядки)
дає щільний, некомфортний блок. Ширина рядка на десктопі — близько 90 символів,
при рекомендованих 60–75.

---

### 2.13. Інтуїтивність — 7 / 10

Основне зрозуміло без пояснень. Неочевидне: що кружечки клікабельні
(немає жодного натяку — ні курсора-підказки на дотику, ні мікроанімації);
що стрічка прокручується вбік (обрізаний 12-й кружечок — єдиний натяк).

---

### 2.14. Візуальна привабливість — 4 / 10

Чесно: нейтрально. Не відразливо, але й не запам'ятовується. Немає жодної
деталі, на якій хочеться затриматись поглядом. Логотип-«гора» — єдиний
елемент з характером, і він 38px у кутку.

---

### 2.15. Відчуття преміальності — 3 / 10

Преміальність у цифрових продуктах будується на трьох речах: **ритм**
(сітка відступів), **глибина** (шари, світло), **деталь** (мікровзаємодії).
Тут відсутні всі три. Продукт відчувається як робочий інструмент, зроблений
охайно, — але не як щось, за що платять.

---

### 2.16. Mobile-first — 6 / 10

**Добре:** тап-цілі (крім `fill-btn`), safe-area, стрічка з прокруткою,
графіки 1:1, поля 16px без авто-зуму.

**Погано:** підхід усе одно desktop-down — базові стилі написані для широкого
екрана, мобільні — через `max-width`. На 500px картка тижня має заголовок,
дві стрілки, кнопку «Сьогодні» і кнопку скидання в одному рядку — тісно.

---

### 2.17. Responsive — 7 / 10

Три обґрунтовані точки зламу, графіки перемальовуються під ширину.
Мінус: між 520px і 1440px рівно нічого не змінюється — вся середня
зона діапазону обділена.

---

### 2.18. Accessibility — 8 / 10

Найбільший стрибок минулої ітерації. ARIA, керування фокусом, Escape,
`prefers-reduced-motion`, контраст. Мінус за `fill-btn` 31px і за те,
що `:focus-visible` не перевірений живими скрінрідерами.

---

### 2.19. Сучасні UI-патерни — 3 / 10

Інвентаризація по файлу:
- градієнти — **0**
- `backdrop-filter` — **0**
- реальні тіні глибини — **1** (і та невидима)
- hover-стани — **7**
- active-стани — **2**
- анімації — **3** keyframes

Для порівняння: у типовому екрані Linear лише станів кнопки чотири
(rest/hover/active/focus) з окремими переходами для кожного.

---

### 2.20. Перше враження — 5 / 10

«Акуратний темний дашборд, зроблений розробником». Читається компетентність,
не читається дизайн.

---

## 3. Найбільші проблеми (за критичністю)

1. **Немає моделі глибини.** 2 тіні з 471 елемента, і єдина справжня —
   `rgba(0,0,0,0.12)` на майже чорному фоні, тобто невидима. Плюс
   **інвертована елевація**: `.metric-row` має фон `rgb(28,27,21)`, а батьківська
   `.hero-card` — `rgb(34,31,25)`. Вкладена поверхня темніша за батьківську,
   хоча в темних інтерфейсах має бути навпаки: чим вище шар, тим світліший.
2. **Немає сітки відступів.** 18 значень, включно з `5px`, `7px`, `11px`, `17.6px`.
3. **`line-height: normal` на всьому.** Вертикального ритму не існує як системи.
4. **`.fill-btn` = 31px** при заявленому мінімумі 44px — регресія попередньої роботи.
5. **Порожній стан домінує.** 11 насичених кружечків важать більше за єдину
   змістовну картку.
6. **Поля вводу — 500px під 4 символи.** Найгірша композиційна помилка.
7. **Жовтий означає три різні речі одночасно.**
8. **Нуль сучасних прийомів глибини й світла** — градієнти, скло, підсвітки.
9. **`h1` і головна метрика однакового розміру** на мобільному.
10. **Пояснення методики на три рядки** щодня під головним числом.

---

## 4. Двадцять чотири конкретні покращення

| # | Що змінити | Навіщо | Пріоритет | Складність |
|---|---|---|---|---|
| 1 | Ввести токени відступів `--sp-1…8` (4/8/12/16/24/32/48/64) і замінити всі 18 поточних значень, включно з `5px`, `7px`, `11px`, `17.6px`, `19.2px` | Сітка відступів — найпомітніша система в преміальному UI; зараз її немає взагалі | Високий | Середньо |
| 2 | Перевернути модель елевації: `.hero-card` → `#1C1B15`, `.metric-row` → `#26241D`, поле вводу → `#2B2822` (кожен наступний шар світліший) | Зараз вкладені поверхні темніші за батьківські — глибина читається навиворіт | Високий | Легко |
| 3 | Замінити `box-shadow: 0 8px 24px rgba(0,0,0,0.12)` на систему з двох шарів: `0 1px 2px rgba(0,0,0,0.4), 0 8px 24px rgba(0,0,0,0.6)` | 12% чорного на фоні `#0F0E0A` не видно фізично — це значення зі світлої теми | Високий | Легко |
| 4 | Задати `line-height`: 1.05 для `--fs-display`, 1.2 для `--fs-xl`, 1.35 для `--fs-md`, 1.5 для `--fs-sm/xs` | `line-height: normal` віддає вертикальний ритм на відкуп браузеру | Високий | Легко |
| 5 | Підняти `.fill-btn` до `min-height: var(--tap)` і прибрати власний фон `--surface-0` | 31px замість 44px, плюс візуальний розлад із сусідньою `.icon-btn` | Високий | Легко |
| 6 | Майбутні тижні в таймлайні — прозорий кружечок з обведенням `1px var(--border-strong)` і сірою цифрою; заливку лишити тільки тижням із даними | 11 насичених кружечків домінують над змістовною карткою | Високий | Легко |
| 7 | Обмежити поле вводу `max-width: 160px`, вирівняти число по правому краю, ліворуч поставити суфікс одиниці («кг», «$») сірим | 500px під 4 символи — найгрубіша композиційна помилка інтерфейсу | Високий | Середньо |
| 8 | Ховати підпис «Weekly Execution Score…» після третього запуску, лишивши іконку-підказку `(?)` | Пояснення методики щодня важить більше за саму метрику | Високий | Середньо |
| 9 | Розвести жовтий: `--warn` (відстаєш) `#E8A93C` і новий `--neutral-progress` `#A8A292` для «у процесі» | Один колір на три різні за смислом стани не дає зчитати, добре це чи погано | Високий | Середньо |
| 10 | На мобільному `h1` → 17px, `.hero-num` → мінімум 32px | Зараз обидва по 22px — назва застосунку конкурує з головною метрикою | Високий | Легко |
| 11 | Увімкнути `font-optical-sizing: auto` і задати `font-variation-settings: 'SOFT' 20, 'WONK' 1` для дисплейних чисел | Fraunces завантажується як варіативний, але жодна вісь не використовується — вага без користі | Середній | Легко |
| 12 | Розвести трекінг: `-0.03em` на 42px, `0` на 16px, `+0.01em` на 11.5px | Зараз `-0.01em` однаково на заголовок і на число — великі кегли потребують щільнішого | Середній | Легко |
| 13 | Додати м'яку радіальну підсвітку за карткою поточного тижня: `radial-gradient(circle at 50% 0%, rgba(47,190,147,0.06), transparent 60%)` | Нуль градієнтів у файлі; один акуратний — дасть глибину без «ефектів» | Середній | Легко |
| 14 | Збільшити відстань між рядками метрик з 8px до 16px, всередині рядка стиснути з 12px до 10px | Три рівноправні сутності зараз не відокремлені візуально | Середній | Легко |
| 15 | Замінити ліві акцентні смужки 3px на кольорову крапку 6px біля підпису або кольоровий підпис | Ліва смужка на картці — патерн Material Design 2018 року | Середній | Легко |
| 16 | Додати `:active` стан усім кнопкам: `transform: scale(0.97)` + перехід 80ms | Зараз 2 active-стани на весь застосунок — дотик не дає тактильного відгуку | Середній | Легко |
| 17 | Мікроанімація появи картки при зміні тижня: `translateY(4px) → 0` + `opacity 0 → 1` за 180ms | Перехід між тижнями зараз миттєвий і дезорієнтує — незрозуміло, що змінилось | Середній | Середньо |
| 18 | Обмежити ширину текстових блоків `max-width: 68ch` | На десктопі рядок сягає ~90 символів при комфортних 60–75 | Середній | Легко |
| 19 | Стрічка тижнів: додати градієнтні маски по краях (`mask-image: linear-gradient(90deg, transparent, black 24px, black calc(100% - 24px), transparent)`) | Зараз єдиний натяк на прокрутку — обрізаний кружечок; маска робить це явним | Середній | Легко |
| 20 | Об'єднати «Налаштувати цілі» й «Налаштувати синхронізацію» в один пункт «Налаштування» з двома секціями | Два пункти меню про одноразове налаштування конкурують із щоденними діями | Середній | Середньо |
| 21 | `--text-muted` підняти з `#8C8977` до `#9A9784` | Зараз 4.66:1 при нормі 4.5 — запас 3%, і саме цим кольором набрано найдрібніший текст | Середній | Легко |
| 22 | Колір капіталу зсунути з `#2FBE93` у бірюзу `#3BC4C4` | Збігається з семантичним зеленим «успіху» — капітал завжди виглядає успішним | Низький | Легко |
| 23 | Додати проміжну точку зламу на 900px: збільшити внутрішні відступи карток і `--fs-display` до 52px | Між 520px і 1440px не змінюється рівно нічого | Низький | Середньо |
| 24 | `backdrop-filter: blur(20px)` + напівпрозорий фон на панелі меню й оверлеях модалок | Єдине доречне місце для скла — плаваючий шар над контентом; зараз панель глуха | Низький | Легко |

---

## 5. Що виглядає застарілим (2018–2022)

1. **Ліва акцентна смужка на картці** — Material Design 2018, зараз майже не вживається.
2. **Плаский дизайн без жодної глибини** — пік моди 2017–2019; сучасний
   мінімалізм працює саме через тонкі шари й світло.
3. **Тінь `rgba(0,0,0,0.12)`** — типове значення Material для світлої теми,
   перенесене в темну без перерахунку.
4. **Іконка «три крапки» для меню** — 2015–2020; зараз частіше явний підпис
   або іконка-шестерня.
5. **Емодзі 🎉 в інтерфейсному тексті** («Випереджаєш план») — 2019–2021.
6. **Пунктирне обведення чекбоксів** (`border: 1px dashed`) — застарілий спосіб
   показати «порожній слот».
7. **`border-radius: 20px` на великих картках** — «дуже заокруглені картки»
   були трендом 2020–2022; у 2025–2026 повертаються стриманіші 12–16px
   на великих поверхнях.
8. **Однорівневий фон** без жодної варіації тону всередині картки.

---

## 6. Що вже виглядає сучасно

1. **Тепла темна база** `#0F0E0A` з жовтуватим підтоном — не банальний
   `#121212`, це смак.
2. **Пара Fraunces + Inter** — свідомий типографічний вибір.
3. **Токенізована система** радіусів, шрифтів, рамок і кольорів.
4. **`tabular-nums` на числах** — деталь професійного рівня.
5. **Горизонтальна стрічка тижнів із прокруткою** — правильний мобільний патерн.
6. **`prefers-reduced-motion`** — рівень зрілості, якого немає в більшості
   комерційних продуктів.
7. **Пігулки статусів** із напівпрозорим фоном — актуальний патерн.
8. **Графіки, що малюються під виміряну ширину** 1:1.

---

## 7. Порівняння з еталонами

| Продукт | Чого не вистачає |
|---|---|
| **Apple** | Оптичного вирівнювання й ритму. У Apple кожен відступ виводиться з розміру шрифту; тут 18 випадкових значень. Плюс матеріальність — у них навіть пласкі поверхні мають ледь помітну варіацію тону. |
| **Linear** | Глибини й швидкості. Linear — теж темний і теж мінімалістичний, але має чітку ієрархію шарів через фон і тіні, і кожна взаємодія має перехід 60–120ms. Тут 2 active-стани й 3 анімації на весь застосунок. |
| **Stripe** | Роботи з даними. Stripe перетворює числа на розповідь: підписи, порівняння, дельти, контекст. Тут числа лежать голими в порожніх полях. |
| **Vercel** | Дисципліни. У Vercel мінімалізм тримається на бездоганній сітці; тут мінімалізм тримається на тому, що просто нічого не додано. |
| **Figma** | Щільності інформації. Figma вміє показати багато на малому просторі, не перевантажуючи. Тут одне число займає 500px. |
| **Arc Browser** | Характеру. В Arc є впізнавана індивідуальність — градієнти, кольорові теми, гумор. Тут єдина індивідуальність — логотип-гора 38px у кутку. |
| **Notion** | Типографічного ритму. Notion — про текст, і в нього кожен рівень має заданий line-height. Тут `line-height: normal` скрізь. |

**Спільний знаменник:** усі сім працюють на трьох рівнях — система (сітка,
токени), матерія (глибина, світло, текстура), деталь (мікровзаємодії).
Трекер має міцний **перший** рівень, майже не має **другого** і практично
не має **третього**.

---

## 8. План розвитку

### Етап 1 — швидкі покращення (2–3 години)
Пункти 2, 3, 4, 5, 6, 10, 11, 12, 14, 16, 21.
Це все — правки CSS без структурних змін: елевація, тіні, line-height,
розмір `fill-btn`, тихий порожній стан, трекінг, active-стани.

**Приріст: 5.4 → 6.8.** Найбільше дасть виправлення елевації й тіней —
інтерфейс уперше отримає глибину.

### Етап 2 — середні покращення (5–7 годин)
Пункти 1, 7, 8, 9, 13, 15, 17, 18, 19, 20, 23.
Токени відступів по всьому файлу, перебудова рядка метрики, розведення
семантики жовтого, мікроанімації переходу між тижнями, об'єднання налаштувань.

**Приріст: 6.8 → 8.0.** Тут з'являється ритм — те, що відрізняє «акуратно»
від «професійно».

### Етап 3 — великий редизайн (15–20 годин)
Переосмислення картки тижня як основної одиниці: замість вертикального
стосу однакових прямокутників — композиція з різною вагою елементів.
Власна ілюстративна мова замість універсальних іконок. Продумана система
станів (порожній / у процесі / виконано / провалено) з окремою візуальною
логікою для кожного. Можливо — перегляд самої метафори «12 кружечків».

**Приріст: 8.0 → 9.0+.** Це рівень, на якому продукт починає виглядати
як зроблений дизайн-командою, а не розробником з гарним смаком.

---

## 9. Застереження про метод

Чесно про межі цієї оцінки:

- Мінімальна ширина вікна Chrome — 500px, тому **до реальних 402px iPhone 17 Pro
  я не дійшов**. Поведінку на 380–460px оцінюю за медіа-запитами й попередніми
  headless-тестами, не за живим рендером.
- Hover-стани оцінені за кодом, не наведенням — у браузерній сесії
  не перевіряв кожен елемент окремо.
- Відчуття від анімацій (плавність, доречність таймінгів) зі скріншотів
  не оцінюється — це треба дивитись у русі.
- Оцінки за критеріями «преміальність» і «візуальна привабливість» суб'єктивні
  за визначенням; я спирався на порівняння з названими еталонами, але інший
  рецензент може розставити їх інакше на ±1 бал.
